import assert from "node:assert/strict";import test from "node:test";import {CONTRACT_VERSION}from"@djamestaft/hermes-herdr-contracts";import{HostBroker,MemoryJobRegistry,type ProjectPolicy}from"./broker.js";import{serveOne}from"./brokerStdio.js";
test("stdio returns one structured response without paths",async()=>{const policy:ProjectPolicy={project:"lmns",repoRoot:"/secret",worktreeRoot:"/secret/jobs",remote:"origin",expectedRemoteUrl:"x",baseRef:"origin/main",baseBranch:"main",githubRepo:"o/r",profiles:["default"]};const broker=new HostBroker(new Map([["lmns",policy]]),new MemoryJobRegistry(),{async create(){return{branch:"jobs/job_12345678",cwd:"/secret/jobs/x"};}},{async create(){return{sessionId:"secret"};},async prompt(){return"ready";},async status(){return"ready";},async read(){return"ok";}});const response=await serveOne(broker,JSON.stringify({contractVersion:CONTRACT_VERSION,verb:"create_job",jobId:"job_12345678",project:"lmns",profile:"default"}));assert.doesNotMatch(response,/\/secret/);});

import assert from "node:assert/strict"; import test from "node:test";
import { HerdrCliAdapter, herdrBinaryForPlatform } from "./herdrCliAdapter.js"; import type { ProcessExecutor, ProcessResult } from "./processExecutor.js";
const out = (body: unknown, exitCode=0, stderr=""): ProcessResult => ({exitCode,stdout:JSON.stringify(body),stderr});
const serverError = (code: unknown, message: unknown, stdout=""): ProcessResult => ({exitCode:1,stdout,stderr:JSON.stringify({error:{code,message}})});
type Invocation={file:string;args:string[];timeoutMs:number|undefined;environment:NodeJS.ProcessEnv|undefined};
class Fake implements ProcessExecutor { public calls: Invocation[] = []; public async run(file: string, args: readonly string[],timeoutMs?:number,environment?:NodeJS.ProcessEnv): Promise<ProcessResult> { this.calls.push({file,args:[...args],timeoutMs,environment}); const cmd = args.slice(0,2).join(" "); if (cmd === "workspace create") return out({result:{workspace:{workspace_id:"w9"}}}); if (cmd === "workspace close") return out({result:{type:"workspace_closed"}}); if (cmd === "api snapshot") return out({result:{snapshot:{panes:[{workspace_id:"w9",pane_id:"w9:p1"}]}}}); if (cmd === "agent start") return out({result:{agent:{agent_status:"idle"}}}); if (cmd === "agent prompt") return out({result:{agent:{agent_status:"working"}}}); if (cmd === "agent get") return out({result:{agent:{agent:"codex",name:args[2],agent_status:"done",interactive_ready:true,pane_id:"w9:p1",workspace_id:"w9"}}}); if (cmd === "pane process-info") return out({result:{process_info:{pane_id:"w9:p1",foreground_processes:[{name:"codex.exe"}]}}}); if (cmd === "agent read") return {exitCode:0,stdout:"safe output",stderr:""}; return out({result:{text:"safe output"}}); } }
const createInput={jobId:"job_12345678",cwd:"/protected/job",profile:"default",label:"task"};
const forbidden=/SECRET_[A-Z0-9_]+|protected|credential|stdout|stderr|arguments?|environment/i;
const messageIs=(expected:string)=>(error:unknown):boolean=>{assert.ok(error instanceof Error);assert.equal(error.message,expected);assert.doesNotMatch(error.message,forbidden);assert.doesNotMatch(JSON.stringify(error),forbidden);return true;};
test("creates a readable workspace and unattended unrestricted Codex agent using exact native Herdr argv", async () => { const fake=new Fake(); const adapter=new HerdrCliAdapter(fake,"codex"); const created=await adapter.create({...createInput,label:"add smoke marker"}); assert.equal(created.sessionId,"discord-add-smoke-mark-12345678"); assert.equal(created.workspaceId,"w9"); assert.ok(fake.calls.every(call=>call.file==="herdr"&&Array.isArray(call.args)&&call.environment===undefined)); assert.deepEqual(fake.calls[0]?.args,["workspace","create","--cwd","/protected/job","--label",created.sessionId,"--no-focus"]); assert.deepEqual(fake.calls[1]?.args,["api","snapshot"]); assert.deepEqual(fake.calls[2]?.args,["agent","start",created.sessionId,"--kind","codex","--pane","w9:p1","--timeout","300000","--","--dangerously-bypass-approvals-and-sandbox","--dangerously-bypass-hook-trust"]); assert.deepEqual(fake.calls[3]?.args,["agent","get",created.sessionId]); assert.deepEqual(fake.calls[4]?.args,["pane","process-info","--pane","w9:p1"]); });
test("resolves the native Herdr executable on Windows without a command shim",()=>{assert.equal(herdrBinaryForPlatform("win32"),"herdr");assert.equal(herdrBinaryForPlatform("darwin"),"herdr");});
test("classifies a hostile executor failure without retaining process context",async()=>{const executor:ProcessExecutor={async run(){const error=Object.assign(new Error("SECRET_THROWN C:\\protected\\herdr credential"),{code:"ENOENT",environment:{TOKEN:"SECRET_TEST_ENV"},argv:["--SECRET_ARGUMENT"]});throw error;}};await assert.rejects(()=>new HerdrCliAdapter(executor,"codex","SECRET_BINARY").create(createInput),messageIs("herdr_workspace_create_failed:exit_other"));});
test("classifies a valid success without a workspace id",async()=>{const executor:ProcessExecutor={async run(){return out({result:{sensitive:"SECRET_STDOUT /protected/workspace"}});}};await assert.rejects(()=>new HerdrCliAdapter(executor,"codex").create(createInput),messageIs("herdr_workspace_create_failed:success_missing_workspace"));});
test("preserves only a normalized previously-unlisted workspace-create server code",async()=>{const executor:ProcessExecutor={async run(){return serverError("  FUTURE_CREATE_CODE  ","SECRET_WORKSPACE_MESSAGE /protected/path credential");}};await assert.rejects(()=>new HerdrCliAdapter(executor,"codex").create(createInput),messageIs("herdr_workspace_create_failed:future_create_code"));});
test("preserves only a normalized snapshot server code",async()=>{const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="api"?serverError("FUTURE_SNAPSHOT_CODE","SECRET_SNAPSHOT_MESSAGE C:\\protected\\pane --arguments"):original(file,args,timeout,environment);await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_snapshot_lookup_failed:future_snapshot_code"));});
test("preserves only the failed start code after bounded at-most-once probes",async()=>{let starts=0,probes=0;const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>{if(args[0]==="agent"&&args[1]==="start"){starts++;return serverError("FUTURE_START_CODE","SECRET_START_MESSAGE /protected/start credential");}if(args[0]==="agent"&&args[1]==="get"){probes++;return serverError("FUTURE_PROBE_CODE","SECRET_PROBE_MESSAGE environment");}return original(file,args,timeout,environment);};await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_agent_start_failed:future_start_code"));assert.equal(starts,1);assert.equal(probes,10);});
const closedFailures: Array<[string,ProcessResult,string]> = [
  ["malformed stderr",{exitCode:1,stdout:"SECRET_STDOUT",stderr:"{SECRET_STDERR"},"exit_1_unstructured"],
  ["non-object stderr",{exitCode:1,stdout:"",stderr:"[]"},"exit_1_unstructured"],
  ["missing error",{exitCode:1,stdout:"",stderr:JSON.stringify({result:{code:"future_code"}})},"exit_1_unstructured"],
  ["wrong-type error",{exitCode:1,stdout:"",stderr:JSON.stringify({error:"SECRET_ERROR"})},"exit_1_unstructured"],
  ["missing code",{exitCode:1,stdout:"",stderr:JSON.stringify({error:{message:"SECRET_MESSAGE"}})},"exit_1_unstructured"],
  ["wrong-type code",{exitCode:1,stdout:"",stderr:JSON.stringify({error:{code:7,message:"SECRET_MESSAGE"}})},"exit_1_unstructured"],
  ["missing message",{exitCode:1,stdout:"",stderr:JSON.stringify({error:{code:"future_code"}})},"exit_1_unstructured"],
  ["wrong-type message",{exitCode:1,stdout:"",stderr:JSON.stringify({error:{code:"future_code",message:7}})},"exit_1_unstructured"],
  ["unsafe code",serverError("future_code/SECRET_RAW_MESSAGE","SECRET_MESSAGE"),"exit_1_unstructured"],
  ["overlength code",serverError(`a${"7".repeat(32)}`,"SECRET_MESSAGE"),"exit_1_unstructured"],
  ["error envelope on stdout",{exitCode:1,stdout:JSON.stringify({error:{code:"future_code",message:"SECRET_STDOUT_MESSAGE"}}),stderr:"SECRET_STDERR"},"exit_1_unstructured"],
  ["syntax exit",{exitCode:2,stdout:"SECRET_STDOUT",stderr:JSON.stringify({error:{code:"future_code",message:"SECRET_SYNTAX_MESSAGE"}})},"exit_2_syntax"],
  ["other exit",{exitCode:17,stdout:"SECRET_STDOUT",stderr:"SECRET_STDERR"},"exit_other"],
  ["malformed success",{exitCode:0,stdout:"{SECRET_STDOUT",stderr:"SECRET_STDERR"},"success_invalid_json"],
  ["non-object success",{exitCode:0,stdout:"[]",stderr:"SECRET_STDERR"},"success_invalid_json"],
  ["malformed result envelope",out({result:["SECRET_RESULT"]}),"success_invalid_json"],
];
for(const [name,response,category] of closedFailures)test(`classifies ${name}`,async()=>{const executor:ProcessExecutor={async run(){return response;}};await assert.rejects(()=>new HerdrCliAdapter(executor,"codex").create({...createInput,cwd:"/protected/SECRET_CWD",label:"SECRET_ARGUMENT"}),messageIs(`herdr_workspace_create_failed:${category}`));});
test("classifies a snapshot with no matching workspace",async()=>{const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="api"?out({result:{snapshot:{panes:[{workspace_id:"other",pane_id:"SECRET_PANE"}]}}}):original(file,args,timeout,environment);await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_snapshot_lookup_failed:success_missing_workspace"));});
test("classifies malformed snapshot and pane structures",async()=>{for(const snapshot of ["SECRET_SNAPSHOT",{panes:"SECRET_PANES"},{panes:["SECRET_PANE"]}]){const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="api"?out({result:{snapshot}}):original(file,args,timeout,environment);await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_snapshot_lookup_failed:success_invalid_json"));}});
test("bounds generated agent names for full public job ids", async () => { const fake=new Fake(); const adapter=new HerdrCliAdapter(fake,"codex"); const created=await adapter.create({jobId:"job_fc2f02db98184f989b2f9109fb4dcc57",cwd:"/protected/job",profile:"default",label:"create a very long descriptive branch name"}); assert.equal(created.sessionId,"discord-create-a-very-fc2f02db"); assert.ok(created.sessionId.length<=32); });
test("isolates automated workspaces from interactive shell startup",async()=>{const fake=new Fake();const adapter=new HerdrCliAdapter(fake,"codex","herdr","/protected/runner-shell");await adapter.create(createInput);assert.deepEqual(fake.calls[0]?.args,["workspace","create","--cwd","/protected/job","--label","discord-task-12345678","--env","ZDOTDIR=/protected/runner-shell","--no-focus"]);});
test("does not repeat startup when the new workspace shell is unavailable",async()=>{let starts=0,probes=0;const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>{if(args[0]==="agent"&&args[1]==="start"){starts++;return serverError("agent_pane_unavailable","shell pending");}if(args[0]==="agent"&&args[1]==="get"){probes++;return serverError("agent_not_found","not found");}return original(file,args,timeout,environment);};await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_agent_start_failed:agent_pane_unavailable"));assert.equal(starts,1);assert.equal(probes,10);});
test("rejects a transient successful start followed by agent exit",async()=>{let starts=0;const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>{if(args[0]==="agent"&&args[1]==="start")starts++;if(args[0]==="agent"&&args[1]==="get")return serverError("agent_not_found","SECRET_STDERR C:\\protected\\agent --arguments credential environment");return original(file,args,timeout,environment);};await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").create(createInput),messageIs("herdr_agent_start_failed:exit_other"));assert.equal(starts,1);});
test("does not send prompt text after the named agent exits",async()=>{const task="SECRET_PROMPT_SENTINEL must never reach a shell";const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="agent"&&args[1]==="get"?serverError("agent_not_found","SECRET_STDERR C:\\protected\\agent --arguments credential environment"):original(file,args,timeout,environment);let caught:unknown;try{await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678",task,10_000);}catch(error){caught=error;}assert.ok(caught instanceof Error);assert.equal(caught.message,"herdr_agent_unavailable");assert.doesNotMatch(JSON.stringify(caught),forbidden);assert.doesNotMatch(JSON.stringify(caught),/SECRET_PROMPT_SENTINEL/);assert.equal(fake.calls.some(call=>call.args[0]==="agent"&&call.args[1]==="prompt"),false);});
test("accepts the Herdr 0.7 live-agent response while retaining process verification",async()=>{const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="agent"&&args[1]==="get"?out({result:{agent:{agent:"codex",agent_status:"done",pane_id:"w9:p1",workspace_id:"w9"}}}):original(file,args,timeout,environment);await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678","task",10_000);assert.equal(fake.calls.filter(call=>call.args[0]==="pane"&&call.args[1]==="process-info").length,2);assert.equal(fake.calls.some(call=>call.args[0]==="agent"&&call.args[1]==="prompt"),true);});
test("rejects mismatched authoritative Herdr 0.8 liveness fields",async()=>{for(const fields of [{name:"other-agent",interactive_ready:true},{name:"discord-task-12345678",interactive_ready:false}]){const task="SECRET_PROMPT_SENTINEL must never be sent";const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="agent"&&args[1]==="get"?out({result:{agent:{agent:"codex",agent_status:"done",pane_id:"w9:p1",workspace_id:"w9",...fields}}}):original(file,args,timeout,environment);await assert.rejects(()=>new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678",task,10_000),messageIs("herdr_agent_unavailable"));assert.equal(fake.calls.some(call=>call.args[0]==="agent"&&call.args[1]==="prompt"),false);}});
test("does not send prompt text after the pane returns to PowerShell even when its argv mentions Codex",async()=>{const task="SECRET_PROMPT_SENTINEL must never reach PowerShell";const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="pane"&&args[1]==="process-info"?out({result:{process_info:{pane_id:"w9:p1",foreground_processes:[{name:"powershell.exe",argv:["powershell.exe","codex.cmd","--dangerously-bypass-hook-trust","SECRET_ARGUMENT"],cmdline:"powershell.exe codex.cmd C:\\protected\\credential",stdout:"SECRET_STDOUT",stderr:"SECRET_STDERR",environment:"SECRET_ENV"}]}}}):original(file,args,timeout,environment);let caught:unknown;try{await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678",task,10_000);}catch(error){caught=error;}assert.ok(caught instanceof Error);assert.equal(caught.message,"herdr_agent_unavailable");assert.doesNotMatch(JSON.stringify(caught),forbidden);assert.doesNotMatch(JSON.stringify(caught),/SECRET_PROMPT_SENTINEL/);assert.equal(fake.calls.some(call=>call.args[0]==="agent"&&call.args[1]==="prompt"),false);});
test("maps Herdr agent states and reads the bounded plain-text transcript", async () => { const fake=new Fake(); const adapter=new HerdrCliAdapter(fake,"codex"); assert.equal(await adapter.status("job-job_12345678"),"ready"); assert.equal(await adapter.read("job-job_12345678",25),"safe output"); assert.deepEqual(fake.calls.at(-1)?.args,["agent","read","job-job_12345678","--source","recent-unwrapped","--lines","25"]); });
test("requires the agent to leave a scoped handoff commit", async () => { const fake=new Fake(); const adapter=new HerdrCliAdapter(fake,"codex"); await adapter.prompt("job-12345678","Make the requested file",10_000); const task=fake.calls.at(-1)?.args[3] ?? ""; assert.match(task,/leave the requested changes committed/); assert.match(task,/Do not include unrelated changes/); });
test("waits for a settled agent state without accepting transient unknown",async()=>{const fake=new Fake();await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678","task",10_000);const call=fake.calls.at(-1)?.args??[];assert.deepEqual(call.slice(0,3),["agent","prompt","discord-task-12345678"]);assert.deepEqual(call.slice(4),["--wait","--timeout","10000"]);assert.equal(call.includes("--until"),false);});
test("requires two bounded live-agent and non-PowerShell process samples before sending a prompt",async()=>{const fake=new Fake();await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678","task",10_000);const promptIndex=fake.calls.findIndex(call=>call.args[0]==="agent"&&call.args[1]==="prompt");assert.ok(promptIndex>0);const checks=fake.calls.slice(0,promptIndex);assert.equal(checks.filter(call=>call.args[0]==="agent"&&call.args[1]==="get").length,2);assert.equal(checks.filter(call=>call.args[0]==="pane"&&call.args[1]==="process-info").length,2);});
test("uses sanitized timeout and workspace-not-found codes for allowed errors",async()=>{const timeoutFake=new Fake();const original=timeoutFake.run.bind(timeoutFake);timeoutFake.run=async(file,args,timeout,environment)=>args[0]==="agent"&&args[1]==="prompt"?serverError("timeout","SECRET_TIMEOUT_MESSAGE"):original(file,args,timeout,environment);assert.equal(await new HerdrCliAdapter(timeoutFake,"codex").prompt("agent","task",1),"unknown");const missingExecutor:ProcessExecutor={async run(){return serverError("workspace_not_found","SECRET_CLOSE_MESSAGE");}};await new HerdrCliAdapter(missingExecutor,"codex").close("agent","workspace");});
test("keeps an approval-blocked agent working instead of allowing handoff",async()=>{const fake=new Fake();const original=fake.run.bind(fake);fake.run=async(file,args,timeout,environment)=>args[0]==="agent"&&args[1]==="prompt"?out({result:{agent:{agent_status:"blocked"}}}):original(file,args,timeout,environment);assert.equal(await new HerdrCliAdapter(fake,"codex").prompt("discord-task-12345678","task",10_000),"working");});
test("closes only the registered Herdr workspace",async()=>{const fake=new Fake();const adapter=new HerdrCliAdapter(fake,"codex");await adapter.close("discord-task-12345678","w9");assert.deepEqual(fake.calls.at(-1)?.args,["workspace","close","w9"]);});

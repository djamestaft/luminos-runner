# Intentionally minimal: automated runner panes must not load interactive
# plugins that prompt for credentials or mutate agent state before Codex starts.
export PATH="$HOME/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin"
